# G3.8H browser integration evidence

Date: 2026-09-05. Branch: `test/paper-monitor-browser`, product baseline `c0376db`. Root owns browser execution, documentation and the CLI example regression; `monitor_browser_fixture` supplied read-only CLI discovery. No external push, credential use or broker order.

## Scope and setup

Flutter 3.47.1 stable / Dart 3.13.1 release JavaScript build, local CanvasKit (`--no-web-resources-cdn`), Playwright CLI 0.1.19, HeadlessChrome/152.0.0.0 on macOS. Browser-reported UA identifies the compatibility platform as Intel Mac OS X 10_15_7, not the actual host OS version. This is functional rendering evidence, not a profile/frame-budget measurement.

The existing `make test` owner wrapper ran Go exact, a temporary browser-session command in place of the Flutter-test executable, then Python 34 tests. The session command built the real Go binary and Flutter entrypoint into its owned temp root, migrated a new DB and served API/web only on loopback ports 18082/18083. A separate named, non-persistent browser was explicitly closed before returning. This does not claim a full `make check` or fresh Flutter unit suite.

Actual CLI path: offline research improvement over the existing research CSV with `SMA` mechanically replaced by `005930`, then `strategy-register`, `strategy-select`, `paper-init`, `paper-import-bars`, and `paper-run-due`. The single explicit-time synthetic daily bar closed after selection/session creation and before import. No direct SQL writes, invented API response, order, arm or continuous runner was needed. The first policy is `INSUFFICIENT`; the policy runner releases its claim and the monitor correctly shows no owned lease. This fixture is not a trading-calendar or profitability observation.

## Observed results

- Connections → monitor loads through the actual cross-origin GET API. Before policy creation the page shows one initialized session and `저장된 정책 결정 없음`; after CLI processing and manual refresh it shows `근거 부족`, its exact stored times and current-selection match.
- Desktop 1440×1100 displays all three cards. Tablet 768×1024 displays light/offline and dark states. At 375×812, scrolling reaches the complete policy card and warnings without horizontal clipping. Screenshots were viewed, not merely generated.
- Offline refresh preserves observation `2026-09-05T08:50:04.71715Z` and both original policy timestamps, with a fixed retained-result warning. Restoring the network and pressing Enter on the focused refresh button removes the warning and advances observation to `2026-09-05T08:52:08.625169Z` without changing policy history.
- Keyboard traversal reaches refresh, the scroll region and Back. Shift-Tab to Back then Enter returns to Connections with focus on the monitor entry. Native dark/reduced-motion media preferences were emulated and reported true; no physical assistive-technology or motion-timing certification is inferred.
- Browser requests were GET-only. The only console errors were the existing expected 404 for absent broker reconciliation and the deliberately induced `ERR_INTERNET_DISCONNECTED` for monitor refresh. Monitor success requests returned 200. No uncaught application error was observed.
- CanvasKit loaded locally, but Flutter dynamically fetched Korean fallback font subsets from `fonts.gstatic.com`. Therefore this session does **not** prove cold-start offline Korean rendering. Build also warned about a referenced Cupertino icon font absent from the asset manifest; the captured web screens rendered their Material icons, but this does not clear iOS icon coverage.

Local ignored screenshots: `output/playwright/paper-monitor-empty-375.png`, `paper-monitor-policy-1440.png`, `paper-monitor-offline-768.png`, `paper-monitor-dark-768.png`, `paper-monitor-policy-dark-375.png`. They are intentional review evidence, not runtime resources. No committed visual baseline exists for this page: visual regression verdict is **INCONCLUSIVE**, while the bounded interaction/rendering checks above passed. No CWV, axe audit, 200% browser zoom or physical VoiceOver/TalkBack evidence was collected here; existing widget coverage remains separate.

## Executable documentation correction

The documented `account_local_paper` failed the real `paper-init` command with `paper accounting session identity is invalid`. Current shared identity requires `kiwoom_account_` plus exactly 24 URL-safe characters even in local paper mode. Keep this legacy identity contract intact and use an explicitly fictitious alias only in a disposable paper DB; it must not be confused with a credentialed broker account. `TestLocalPaperWorkflowAccountExamplesCanInitialize` extracts each documented `-account` value and executes the existing initialization command with real validated research/session prerequisites. Its fixture already owns the corrected alias, so this test proves document-linked identity acceptance and idempotent initialization; the existing `TestLocalPaperCLIInitializeAndImport` separately covers fresh creation, which was also exercised in this browser session.

RED: the documentation regression failed on `account_local_paper` with the same identity error (core 0.417s, wrapper exit 2). GREEN: `GOFLAGS='-race -run=TestLocalPaperWorkflowAccountExamplesCanInitialize -count=1' make check FLUTTER=true` passed (core 1.793s; Python 34/4.064s; vet, formatting, 17 JSON contracts, cleanup). No production Go or Flutter implementation changed, so the prior full suite was not repeated.

Final browser wrapper exited zero (Python 34/4.035s). Its exact API/web PIDs 68662/68663 and browser daemon PID 72204 were absent afterward, ports 18082/18083 had no listener, named browser inventory was empty, and owned `omni-folio-test.OmaVm7` was absent. Repository build/coverage outputs were also absent. Initial wrapper setup failures (a quoted executable-with-arguments override and unavailable background stdin) exited through cleanup before the successful bounded marker-wait run; they are harness failures, not product RED evidence. No Podman/Kind resources were created. The temporary QA script is ignored local tooling, not a supported new command or a general browser SIGKILL-recovery guarantee.

## Research and interpretation

A focused Exa query returned official Flutter guidance; two official pages were read: [web accessibility](https://docs.flutter.dev/ui/accessibility/web-accessibility) and [accessibility testing](https://docs.flutter.dev/ui/accessibility/accessibility-testing). Flutter renders on canvas and exposes a separate opt-in semantic DOM. We activated its existing `Enable accessibility` placeholder for automation, then compared screenshots with the semantic snapshot. Inference: a correct widget semantics test or semantic DOM is not independently proof of painted layout or human screen-reader usability. No runtime semantics override, new dependency or design-system replacement was introduced. The UI skill's keyboard-navigation guidance was applied to refresh and return navigation.
